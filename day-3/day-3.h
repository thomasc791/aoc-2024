#include "scanner.h"
#include <fstream>
#include <sstream>
#include <vector>

std::vector<std::string> readinput(std::string file);
int part_1(Scanner &scanner);
int part_2(Scanner &scanner);
int main();
